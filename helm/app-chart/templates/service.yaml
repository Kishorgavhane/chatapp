apiVersion: v1
kind: Service
metadata:
  name: chatapp-service
spec:
  selector:
    app: chatapp
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 3000
  type: {{ .Values.service.type }}
