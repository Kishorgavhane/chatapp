# ansible/inventories/staging.ini
# EKS tasks run locally via kubectl (connection: local)
[local]
localhost ansible_connection=local

[eks_nodes]
# Add EC2 worker node IPs here if you need direct node access
# node1.us-east-1.compute.internal ansible_user=ec2-user
